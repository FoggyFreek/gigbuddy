// Current Terms & Conditions version, shared verbatim by server (acceptance
// validation) and frontend (the documents in src/content/terms/). Bump the
// date when the terms text changes materially; users are then re-prompted.
export const TERMS_VERSION = '2026-07-08'
