// Shared OpenStreetMap tile config for every Leaflet map in the app
// (GigWorldMap, GigLocationMap, StaticLocationMap). OSM tile usage requires
// visible attribution — keep it in every mode.
export const OSM_URL = 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png'
export const OSM_ATTRIBUTION =
  '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
